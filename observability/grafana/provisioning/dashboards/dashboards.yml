apiVersion: 1

providers:
  - name: OSShip
    orgId: 1
    folder: OSShip
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
