{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [{ "expr": "sum(rate(http_requests_total{service=\"gateway\"}[5m])) by (route)", "legendFormat": "{{route}}" }],
      "title": "Gateway Requests by Route",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [{ "expr": "sum(rate(rate_limit_exceeded_total[5m])) by (route)", "legendFormat": "{{route}}" }],
      "title": "Rate Limit Hits",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["osship", "gateway"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "Gateway",
  "uid": "osship-gateway",
  "version": 1
}
